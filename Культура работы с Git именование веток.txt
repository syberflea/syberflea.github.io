Культура работы с Git: именование веток
Юлия Волкова, фронтенд-разработчик, ментор • 7 февраля в 16:35




Культура работы с Git — это в том числе и культура работы с ветками: начиная от подхода к организации веток в проекте и заканчивая тем, как мы их именуем.

Сегодня мы обсудим один конкретный и весьма практический аспект — рекомендации по именованию веток, и снова увидим иллюстрацию того, как осознанный подход даже к такой утилитарной вещи, как имя ветки, может делать работу в команде более продуктивной и комфортной.

Правильное именование веток в Git:
обеспечивает чистоту и удобство управления версиями в проекте. Например, специальные префиксы feature или hotfix могут указывать на масштаб изменений и влияние добавляемой функциональности на продукт, то есть характеризовать MINOR или PATCH изменения в концепции семантического версионирования (или семвер, как его еще иногда называют сокращенно). Если хотите узнать про семвер больше, рекомендую статью на Хабре. 
облегчает навигацию в проекте. По информативным и понятным названиям веток легче искать изменения и даже находить отсылки к конкретным задачам из таск-трекера (например, из Jira).
помогает настраивать автоматизацию на ветки, названные в соответствии с определёнными конвенциями: например, доставлять изменения на те или иные окружения, создавать docker образы, прогонять линты и тесты, отправлять уведомления.
Общепринятые рекомендации по именованию:
Используйте короткие, но описательные имена. Имя должно быть информативным, чтобы по нему можно было понять, над чем ведётся работа. Пусть оно будет достаточно коротким, чтобы его было легко запомнить и удобно вводить в поиске IDE или GitHub’а.
Делите слова в имени ветки дефисами или нижним подчёркиванием, придерживайтесь единообразия. Это общепринятый подход, лучше не пользоваться пробелами или camelCase. Если вы используете дефисы для разделения слов в имени ветки, старайтесь всегда именовать ветки именно так, избегайте смешивания подходов. 
Удачные именования

✅feature-add-login-page 

✅feature/add-login-page 

✅ feature/PROJ-123-add-login-page

В этом случае символ / допустим, потому что выделяет префикс, — это общепринятый подход.



Неудачные именования

❌feature-add_login_page

❌feature_add_login-page

❌FeatureAddLoginPage

❌feature-AddLoginPage

❌feature_proj-123_AddLoginPage

Уже сложнее читать, труднее настраивать автоматизацию — слишком много разномастных символов и паттернов.

Старайтесь не использовать спецсимволы: @, <, >, $.  Из-за них иногда могут ломаться скрипты автоматизации.
Используйте префиксы для разных типов веток. Префиксы помогают быстро идентифицировать тип ветки и характер изменений. Общепринятые префиксы:
💡feature/ для новых функций (например, feature/new-login-form),

💡bugfix/ или fix/ для исправлений ошибок (например, fix/login-error),

💡hotfix/ для срочных исправлений прямо в рабочей ветке,

💡release/ для подготовки релизов (например, release/1.0.1),

💡refactor/ для структурных изменений кода, не влияющих на функциональность,

💡docs/ для обновлений документации.

Включите в имя идентификатор задачи. Если ваш проект использует систему управления задачами (например, Jira), добавьте её идентификатор. Это облегчит отслеживание задач и связанных с ними изменений в коде. Пример: feature/PROJ-123-add-login-page.
Избегайте слишком общих имён. Например, fix или update слишком общие и не дают чёткого представления о содержании ветки. Постарайтесь быть более специфичными.
Придерживайтесь единого подхода всей командой. Договоритесь с коллегами, ведь согласованность в именовании облегчает ориентацию в ветках и управление ими, особенно в больших проектах.
Избегайте использования служебных слов Git в качестве префиксов. Слова типа head, master, tag могут вызвать путаницу, если пользоваться ими в именованиях.
Следование этим рекомендациям поможет поддерживать порядок в репозитории и упростит навигацию по веткам для всей команды.